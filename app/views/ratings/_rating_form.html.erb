<%= form_for([@idea, @rating]) do |f| %>

  <div class="field">
    <%= f.label :mark %><br>
    <%= f.select :mark, [1,2,3,4,5,6] %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
